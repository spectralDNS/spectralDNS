package:
  name: spectraldns
  version: "{{ GIT_DESCRIBE_TAG }}"

source:
  git_url: ../../

build:
  number: 35

requirements:

  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}

  host:
    - python
    - cython
    - numpy
    - setuptools
    - nomkl
    - fftw
    - mpi4py
    - mpi4py-fft
    - {{ mpi }}
    - pytest-runner
    - pythran
    - pip

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - nomkl
    - fftw
    - {{ mpi }}
    - hdf5 * mpi_{{ mpi }}_*
    - h5py * mpi_{{ mpi }}_*
    - shenfun
    - mpi4py-fft
    - mpi4py
    - scipy
    - sympy
    - pythran
    - pip

test:
  source_files:
    - tests

  imports:
    - spectralDNS
    - shenfun

  requires:
    - pytest

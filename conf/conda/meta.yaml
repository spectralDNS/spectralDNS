package:
  name: spectraldns
  version: "1.0"
  
source:
  git_url: ../../  
  
build:
  number: 2

requirements:
  build:
    - python
    - cython
    - numpy

  run:
    - python
    - numpy
    - scipy
    - h5py ==2.6.0
    - mpi4py
    - mpifft4py
    - sympy

test:
    requires:
      - python
      - pytest
